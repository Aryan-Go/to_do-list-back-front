<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="flex flex-row justify-start items-center">
        <% rows.forEach(row =>{%>
            <div class="flex flex-col justify-center items-center w-max h-max bg-red-50 m-[1rem] p-[1rem]">
                <div class="font-bold text-3xl"><%= row.title %></div>
                <div><%= row.description %></div>
            </div>
        <% }) %>
    </div>
    <form action="/todo_add">
        <button class="bg-" type="submit">Add new task</button>
    </form>
</body>
</html>